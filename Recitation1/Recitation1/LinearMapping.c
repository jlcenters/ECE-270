#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
/*
main() {
	float inMin, inMax, outMin, outMax, in, out;

	printf("Please enter the input range: "); //inmin inmax
	scanf("%f %f", &inMin, &inMax);

	printf("Please enter the output range: "); //outmin outmax
	scanf("%f %f", &outMin, &outMax);

	printf("Please enter the input value: "); //in (should be within input range)
	scanf("%f", &in);

	if (in <= inMax && in >= inMin) {
		out = ((outMax - outMin) / (inMax - inMin)) * (in - inMin) + outMin; //out
		printf("The output of %f is %f.", in, out);
	}
	else {
		printf("Given value is not within range.");
	}
}
*/